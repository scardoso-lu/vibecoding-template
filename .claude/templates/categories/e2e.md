# E2E Sections

Include these sections in `slice.md` only for user-facing slices. The explorer writes only
`e2e/report.md` and artifacts.

```md
## E2E Exploration
- Launch backend:
- Launch frontend:
- Seed data / credentials:
- Flows to explore:
- Edges to probe:
```
